name: Check Upstream Updates

on:
  schedule:
    # 매주 월요일 오전 9시 (KST) = 매주 월요일 자정 (UTC)
    - cron: '0 0 * * 1'
  workflow_dispatch:

jobs:
  check-updates:
    runs-on: ubuntu-latest
    permissions:
      issues: write
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Add upstream remote
        run: |
          git remote add upstream https://github.com/nibzard/awesome-agentic-patterns.git || true
          git fetch upstream main

      - name: Check for new commits
        id: check
        run: |
          # Get the latest commit from upstream
          UPSTREAM_COMMIT=$(git rev-parse upstream/main)
          echo "upstream_commit=$UPSTREAM_COMMIT" >> $GITHUB_OUTPUT

          # Check if this commit exists in our repo
          if git merge-base --is-ancestor $UPSTREAM_COMMIT HEAD; then
            echo "has_updates=false" >> $GITHUB_OUTPUT
            echo "No new updates from upstream"
          else
            echo "has_updates=true" >> $GITHUB_OUTPUT

            # Get the common ancestor
            MERGE_BASE=$(git merge-base HEAD upstream/main)

            # Count new commits
            NEW_COMMITS=$(git rev-list --count $MERGE_BASE..upstream/main)
            echo "new_commits=$NEW_COMMITS" >> $GITHUB_OUTPUT

            # Get changed files in patterns/ directory
            CHANGED_FILES=$(git diff --name-only $MERGE_BASE..upstream/main -- patterns/ | head -20)
            echo "changed_files<<EOF" >> $GITHUB_OUTPUT
            echo "$CHANGED_FILES" >> $GITHUB_OUTPUT
            echo "EOF" >> $GITHUB_OUTPUT

            # Get commit log
            COMMIT_LOG=$(git log --oneline $MERGE_BASE..upstream/main --max-count=10)
            echo "commit_log<<EOF" >> $GITHUB_OUTPUT
            echo "$COMMIT_LOG" >> $GITHUB_OUTPUT
            echo "EOF" >> $GITHUB_OUTPUT
          fi

      - name: Create Issue for Updates
        if: steps.check.outputs.has_updates == 'true'
        uses: actions/github-script@v7
        with:
          script: |
            const newCommits = '${{ steps.check.outputs.new_commits }}';
            const changedFiles = `${{ steps.check.outputs.changed_files }}`;
            const commitLog = `${{ steps.check.outputs.commit_log }}`;
            const upstreamCommit = '${{ steps.check.outputs.upstream_commit }}';

            const body = `## 🔄 Upstream 업데이트 감지

            upstream 저장소([nibzard/awesome-agentic-patterns](https://github.com/nibzard/awesome-agentic-patterns))에 새로운 변경사항이 있습니다.

            ### 📊 요약
            - **새 커밋 수**: ${newCommits}개
            - **최신 커밋**: \`${upstreamCommit.substring(0, 7)}\`
            - **확인 링크**: [Compare changes](https://github.com/nibzard/awesome-agentic-patterns/compare/${upstreamCommit.substring(0, 7)}...main)

            ### 📝 최근 커밋 로그
            \`\`\`
            ${commitLog}
            \`\`\`

            ### 📂 변경된 파일 (patterns/ 디렉토리)
            ${changedFiles ? '```\n' + changedFiles + '\n```' : '_변경된 패턴 파일이 없습니다_'}

            ### 🔧 다음 단계
            1. [upstream 저장소](https://github.com/nibzard/awesome-agentic-patterns) 확인
            2. 새로운 패턴이 있다면 CLAUDE.md의 "upstream에서 새 패턴 추가하기" 가이드 참고
            3. 패턴 번역 및 JSON 파일 생성
            4. 빌드 테스트 후 커밋

            ---
            _이 이슈는 자동으로 생성되었습니다. 작업 완료 후 이슈를 닫아주세요._`;

            await github.rest.issues.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title: '🔄 Upstream 업데이트 감지',
              body: body,
              labels: ['upstream', 'sync']
            });

      - name: No Updates Found
        if: steps.check.outputs.has_updates == 'false'
        run: |
          echo "✅ Upstream is up to date. No action needed."
